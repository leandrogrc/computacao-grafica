#pragma once

// Teclas
extern bool keyW;
extern bool keyA;
extern bool keyS;
extern bool keyD;
extern bool keyShift;
extern bool keyG;

// Mouse / warp
extern bool ignoreWarp;
extern bool firstMouse;
